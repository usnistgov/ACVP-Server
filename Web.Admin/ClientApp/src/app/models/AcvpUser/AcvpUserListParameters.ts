import { PagedEnumerable } from '../responses/PagedEnumerable';

export class AcvpUserListParameters extends PagedEnumerable {

  // This one does not support filtering, so it's just this for now

  public constructor() {
    super();
  }
}
